include 'backend'
include 'frontend'
include 'examples'

rootProject.name = 'ArchitectFX'
rootProject.children.each { project ->
    {
        project.projectDir = file("modules/$project.name")
    }
}

